# Copy this file to .env and fill in real values.
# Never commit .env (it is gitignored).

# MongoDB connection string.
# Example (do NOT commit secrets): mongodb+srv://USER:PASSWORD@HOST/?appName=CLUSTER_NAME
MONGODB_URI=mongodb+srv://<username:<password>@<host>/?appName=<CLUSTER_NAME>